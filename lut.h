//this is a lookup table that maps exponential values N, expressed as
//scaled integers, to 2^N. In this scheme, a value of 1024 corresponds
//to an exponent N of 8. This is a hack to use fixed-point values to
//calculate exponentials

const uint8_t ocr_LUT[2048] PROGMEM {
    //values
    23
}
